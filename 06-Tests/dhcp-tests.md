# Tests du Service DHCP

* **Vérification sur un poste client :** Exécution de la commande `ipconfig /all`.
* **Résultats :** 
  * Attribution d'une adresse IP dynamique dans la plage (ex: `192.168.1.50`).
  * Le serveur DNS configuré dynamiquement est bien `192.168.1.10`.
* **Statut :** Validé avec succès.
